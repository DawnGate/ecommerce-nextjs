import { memo } from 'react'
import { Swatch } from '@components/product'
import type { ProductOption } from '@commerce/types'
import { SelectedOptions } from '../helpers'

const ProductionOptions = () => {
	return <div></div>
}

export default memo(ProductionOptions)
